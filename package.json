{
  "name": "relive-frontend",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint . --ext .js && stylelint \"**/*.css\" && prettier --check .",
    "test": "jest",
    "cy:run": "cypress run --headless",
    "lhci": "lhci autorun",
    "ci": "npm run lint && npm test && npm run cy:run && npm run lhci"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "stylelint": "^14.0.0",
    "jest": "^29.0.0",
    "cypress": "^12.0.0",
    "@lhci/cli": "^0.10.0"
  }
}
